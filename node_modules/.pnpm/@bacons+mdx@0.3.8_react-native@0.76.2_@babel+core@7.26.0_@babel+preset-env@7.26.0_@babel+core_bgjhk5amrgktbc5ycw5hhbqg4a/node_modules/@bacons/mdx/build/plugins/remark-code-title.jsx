"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const unist_util_visit_1 = __importDefault(require("unist-util-visit"));
/**
 * This simple plugin appends the code block meta to the node value.
 */
function remarkLinkRewrite() {
    return (tree, file) => {
        if (!file.cwd || !file.history || !file.history.length) {
            return;
        }
        (0, unist_util_visit_1.default)(tree, "code", (node) => {
            if (node.meta) {
                node.value = "@@@" + node.meta + "@@@" + node.value;
            }
        });
    };
}
exports.default = remarkLinkRewrite;
// @ts-ignore
module.exports = remarkLinkRewrite;
//# sourceMappingURL=remark-code-title.jsx.map